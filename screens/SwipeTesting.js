import React from 'react';
import {
  View,
  Text,
  StyleSheet,
  Dimensions,
  Animated,
  TouchableOpacity,
} from 'react-native';
import Swipeable from 'react-native-gesture-handler/Swipeable';

// const SCREEN_WIDTH = Dimensions.get('window').width;

const SwipeTesting = (props) => {
    return ( <>
    </> );
}
 
export default SwipeTesting;